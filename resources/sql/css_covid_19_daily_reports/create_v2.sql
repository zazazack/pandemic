CREATE TABLE IF NOT EXISTS csse_covid_19_daily_reports_v2(
    `Province/State` STRING,
    `Country/Region` STRING,
    `Last Update` STRING,
    Confirmed STRING,
    Deaths STRING,
    Recovered STRING,
    Latitude STRING,
    Longitude STRING
)
